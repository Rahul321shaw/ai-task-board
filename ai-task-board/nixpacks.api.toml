[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm@10.33.0", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @ai-task-board/api db:generate", "pnpm --filter @ai-task-board/api build"]

[start]
cmd = "node apps/api/dist/index.js"
